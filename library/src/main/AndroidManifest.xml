<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.haichecker.lib">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        >
        <provider
            android:name=".download.db.DownLoadContentProvider"
            android:authorities="com.haichecker.lib.download.db.DownLoadContentProvider"/>
    </application>

</manifest>
